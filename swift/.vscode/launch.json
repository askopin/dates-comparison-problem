{
    "configurations": [
        {
            "type": "swift",
            "request": "attach",
            "name": "Attach to Swift Executable",
            "pid": "${command:pickProcess}"
        }

    ]
}